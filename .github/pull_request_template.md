## Summary

<!-- 1-3 bullets on what this PR changes and why. -->

## Test plan

- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] Manual smoke test: <!-- describe what you ran in the browser / CLI to verify the user-visible behavior -->
- [ ] No real financial data staged: `git status` and `git diff --cached` clean of `*.csv`, `*.xlsx`, files under `data/`/`transactions/`/`sheets/`

## Closes

Closes #<!-- issue number -->

---
*Co-authored by Claude*
